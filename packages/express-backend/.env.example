# Each contributor should create their own .env file inside the express-backend/ folder
# Create a database user on MongoDB web interface
# Replace <db_username> and <db_password> with the username and password of the database user you created

MONGODB_URI=mongodb+srv://<db_username>:<db_password>@fantastic-five.wktar1f.mongodb.net/?appName=fantastic-five